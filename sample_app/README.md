# Sample app

Sample geolocalisation application

## Bower install
Run
    ~$ npm install
    ~$ bower install

## Karma install
    ~$ npm install -g karma
    ~$ npm install -g karma-cli

## Protractor install
    ~$ npm install -g protractor
    
## Tests
Run
    ~$ karma start

