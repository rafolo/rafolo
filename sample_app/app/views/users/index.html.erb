<% provide(:title, t2('Users')) %>
<% provide(:selected_menu_icon, 'icon-sitemap') %>

<div class="row">
  <div class="col-md-10">
    <div class="box">
        <div class="box-content">
            <%= render @users %>
        </div>
    </div>
    <div class="text-center">
      <%= will_paginate @collection, renderer: BootstrapPagination::Rails %>
    </div>
  </div>
</div>
