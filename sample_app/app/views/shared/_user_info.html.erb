<a href="<%= user_path(current_user) %>">
  <div class="avatar">
    <%= gravatar_for current_user, size: 52 %>
  </div>
</a>
<h1><%= current_user.name %></h1>
<strong><%= link_to "view my profile", current_user %></strong>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>